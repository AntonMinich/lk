# Кабинет партнёра fincode

Два кабинета в одном приложении:

- **Кабинет партнёра** — `/` вход, `/register` заявка, `/cabinet` после одобрения
- **Админка** — `/admin` вход сотрудников

## Кабинет партнёра

После одобрения заявки на регистрацию партнёр входит в кабинет. В шапке кнопка **Создать заявку** открывает форму (организация, контакт, телефон, предмет лизинга, сумма, срок). **Отправить заявку** сохраняет её со статусом «На рассмотрении»; список своих заявок виден на главной кабинета, в админке они появляются в разделе **Заявки на лизинг**.

## Админка

После входа (`admin` / `fincode` или `admin2` / `fincode2`) слева меню разделов, сверху справа — имя сотрудника и иконка. По клику на иконку: **Мой профиль** и **Выйти**. Сессия сохраняется в браузере: обновление страницы не выкидывает из кабинета.

- Заявки на регистрацию партнера — `/admin/partners`
- Заявки на лизинг — `/admin/leasing`
- Профиль сотрудника — `/admin/profile`

Клик по строке открывает карточку заявки. Справа — ответственный менеджер, кнопки **Принять**, **Одобрить**, **Отклонить**, **Сменить менеджера** и ссылка **История**. После принятия заявка «В работе», после одобрения — «Активен».

## Страницы

- Вход партнёра: `/`
- Регистрация: `/register`
- Кабинет партнёра: `/cabinet`
- Новая заявка на лизинг: `/cabinet/applications/new`
- Вход в админку: `/admin`

## Запуск на своём компьютере

```bash
npm install
npm run dev
```

Затем http://localhost:5173
